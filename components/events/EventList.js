const EventList = ({ items }) => {
    return (
        <div>
            <ul>
                {items.map((item) => (
                    <li>{}</li>
                ))}
            </ul>
        </div>
    )
}

export default EventList
